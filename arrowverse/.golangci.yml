linters-settings:
  goimports:
    # Put imports beginning with prefix after 3rd-party packages; it's a comma-separated list of prefixes
    local-prefixes: go.jlucktay.dev/golang-workbench/arrowverse
